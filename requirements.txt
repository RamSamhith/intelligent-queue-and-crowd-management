# VisionQueue Computer Vision Subsystem Dependencies
# Python 3.11+
#
# CUDA acceleration is provided natively by ONNX Runtime with NVIDIA runtime
# packages (CUDA 13 + cuDNN 9), installed via the [cuda,cudnn] extra.
# No standalone CUDA Toolkit or PyTorch installation is required.

# Core Computer Vision & Deep Learning Runtime
numpy>=1.26.0,<3.0.0
opencv-python>=4.8.0,<6.0.0
onnxruntime-gpu[cuda,cudnn]>=1.29.0,<2.0.0

# REST API & Networking
fastapi>=0.100.0
uvicorn>=0.25.0
httpx>=0.24.0

# Testing & Quality Assurance
pytest>=8.0.0
scipy>=1.11.0,<2.0.0

